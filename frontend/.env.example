# Minimal env template – only vars used by the app/API
# Copy to .env.local (local dev) or add in Vercel Project Settings

# Public (browser-exposed)
NEXT_PUBLIC_SITE_URL=https://zyura.vercel.app
NEXT_PUBLIC_SOLANA_NETWORK=devnet
NEXT_PUBLIC_USDC_MINT=4sCh4YUdsFuUFTaMyAx3SVnHvHkY9XNq1LX4L6nnWUtv

# Flight Delay API (used by /api/zyura/delay-summary)
# If not set, MOCK_DELAY_MINUTES will be used
FLIGHT_API_BASE=
FLIGHT_API_KEY=
MOCK_DELAY_MINUTES=

# GitHub metadata upload (used by API route and scripts if you choose)
GITHUB_TOKEN=
GITHUB_REPO=
GITHUB_BRANCH=main
GITHUB_PATH=metadata

# Flight Metadata Repo (separate from NFT metadata)
GITHUB_FLIGHT_REPO=alienx5499/zyura-flight-metadata
